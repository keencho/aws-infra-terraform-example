{
  "name": "app-user",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build:test": "vite build --mode test",
    "build:production": "vite build --mode production"
  },
  "dependencies": {
    "app-common": "^1.0.0"
  }
}
